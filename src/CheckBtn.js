import React, {useState, useEffect} from 'react';
import {
  Button,
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  TouchableOpacity,
  useColorScheme,
  View,
} from 'react-native';

const CheckBtn = () => {
  return (
    <TouchableOpacity>
      <Text>出席</Text>
    </TouchableOpacity>
  );
};

export default CheckBtn;
